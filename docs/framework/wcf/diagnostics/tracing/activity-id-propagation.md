---
title: Propagação de ID de atividade
ms.date: 03/30/2017
ms.assetid: cd1c1ae5-cc8a-4f51-90c9-f7b476bcfe70
ms.openlocfilehash: 0f0478b16bf2ca0975ae0290a8855756ecfc383e
ms.sourcegitcommit: bc293b14af795e0e999e3304dd40c0222cf2ffe4
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/26/2020
ms.locfileid: "96236098"
---
# <a name="activity-id-propagation"></a><span data-ttu-id="ff9ae-102">Propagação de ID de atividade</span><span class="sxs-lookup"><span data-stu-id="ff9ae-102">Activity ID Propagation</span></span>

<span data-ttu-id="ff9ae-103">A propagação ocorre quando o rastreamento de atividade ServiceModel está habilitado (propagação de ServiceModel) ou desabilitado (propagação de atividade de usuário para usuário).</span><span class="sxs-lookup"><span data-stu-id="ff9ae-103">Propagation happens when ServiceModel activity tracing is enabled (ServiceModel propagation) or disabled (User-to-User activity propagation).</span></span>  
  
## <a name="servicemodel-activity-tracing-is-enabled"></a><span data-ttu-id="ff9ae-104">Rastreamento de atividade de ServiceModel está habilitado</span><span class="sxs-lookup"><span data-stu-id="ff9ae-104">ServiceModel Activity Tracing is Enabled</span></span>  

 <span data-ttu-id="ff9ae-105">Se o ServiceModel ActivityTracing estiver habilitado, a propagação ocorrerá entre as atividades processAction.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-105">If ServiceModel ActivityTracing is enabled, propagation happens between ProcessAction activities.</span></span>  
  
### <a name="server"></a><span data-ttu-id="ff9ae-106">Servidor</span><span class="sxs-lookup"><span data-stu-id="ff9ae-106">Server</span></span>  

 <span data-ttu-id="ff9ae-107">Quando o `propagateActivity` atributo é definido como `true` no cliente e no servidor, a ID da `ProcessAction` atividade no servidor é idêntica à ID no cabeçalho da mensagem propagada `ActivityId` .</span><span class="sxs-lookup"><span data-stu-id="ff9ae-107">When the `propagateActivity` attribute is set to `true` on both the client and server, the ID of the `ProcessAction` activity in the server is identical to the ID in the propagated `ActivityId` message header.</span></span>  
  
 <span data-ttu-id="ff9ae-108">Quando nenhum `ActivityId` cabeçalho estiver presente na mensagem (ou seja, `propagateActivity` = `false` no cliente) ou quando estiver `propagateActivity` = `false` no servidor, o servidor gerará uma nova ID de atividade.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-108">When no `ActivityId` header is present in the message (that is, `propagateActivity`=`false` on the client), or when `propagateActivity`=`false` on the server, the server generates a new activity ID.</span></span>  
  
### <a name="client"></a><span data-ttu-id="ff9ae-109">Cliente</span><span class="sxs-lookup"><span data-stu-id="ff9ae-109">Client</span></span>  

 <span data-ttu-id="ff9ae-110">Se o cliente tiver um thread único de forma síncrona, o cliente desconsiderará todas as configurações do `propagateActivity` no cliente ou servidor.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-110">If the client is synchronously single threaded, the client disregards any settings of `propagateActivity` at the client or server.</span></span> <span data-ttu-id="ff9ae-111">Em vez disso, a resposta é tratada na atividade de solicitação.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-111">Instead, the response is handled in the request activity.</span></span> <span data-ttu-id="ff9ae-112">Se o cliente for assíncrono ou síncrono com multithread, `propagateActivity` = `true` no cliente e houver um cabeçalho de ID de atividade na mensagem enviada pelo servidor, o cliente recuperará a ID da atividade e transferirá para a atividade processar ação que contém a ID da atividade propagada.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-112">If the client is asynchronous or synchronous multithreaded, `propagateActivity`=`true` in the client, and there is an activity ID header in the message sent by the server, the client retrieves the activity ID from the message, and transfers to the Process Action activity that contains the propagated activity ID.</span></span> <span data-ttu-id="ff9ae-113">Caso contrário, o cliente transfere da atividade processar mensagem para uma nova atividade processar ação.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-113">Otherwise, the client transfers from Process Message activity to a new Process Action activity.</span></span> <span data-ttu-id="ff9ae-114">Essa atividade de transferência extra para uma nova ação de processo é feita para consistência.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-114">This extra transfer to a new Process Action activity is done for consistency.</span></span> <span data-ttu-id="ff9ae-115">Dentro dessa atividade, o cliente recupera a ID de atividade da atividade BeginCall do contexto de thread local, quando o thread é alocado para o processamento de mensagens de resposta.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-115">Inside this activity, the client retrieves the activity ID of the BeginCall activity from the local thread context, when the thread is allocated for response message processing.</span></span> <span data-ttu-id="ff9ae-116">Em seguida, ele transfere para a atividade de ação de processo inicial.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-116">It then transfers to the initial Process Action activity.</span></span>  
  
 <span data-ttu-id="ff9ae-117">Se o cliente for duplex, o cliente atuará como servidor ao receber a mensagem.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-117">If the client is duplex, the client acts as server on receiving the message.</span></span>  
  
### <a name="propagation-in-fault-messages"></a><span data-ttu-id="ff9ae-118">Propagação em mensagens de falha</span><span class="sxs-lookup"><span data-stu-id="ff9ae-118">Propagation in Fault Messages</span></span>  

 <span data-ttu-id="ff9ae-119">Não há nenhuma diferença no tratamento de mensagens válidas e de falha.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-119">There is no difference in handling valid and fault messages.</span></span> <span data-ttu-id="ff9ae-120">Se `propagateActivity` = `true` , a ID da atividade adicionada aos cabeçalhos de mensagem de falha SOAP é idêntica à atividade ambiente.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-120">If `propagateActivity`=`true`, the activity ID added to the SOAP fault message headers is identical to the ambient activity.</span></span>  
  
## <a name="servicemodel-activity-tracing-is-disabled"></a><span data-ttu-id="ff9ae-121">Rastreamento de atividade de ServiceModel desabilitado</span><span class="sxs-lookup"><span data-stu-id="ff9ae-121">ServiceModel Activity Tracing is Disabled</span></span>  

 <span data-ttu-id="ff9ae-122">Se o ServiceModel ActivityTracing estiver desabilitado, a propagação ocorrerá entre as atividades de código do usuário diretamente sem passar pelas atividades de ServiceModel.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-122">If ServiceModel ActivityTracing is disabled, propagation occurs between user code activities directly without going through ServiceModel activities.</span></span> <span data-ttu-id="ff9ae-123">Uma ID de atividade definida pelo usuário também é propagada por meio do cabeçalho de ID de atividade da mensagem.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-123">A user-defined activity ID is also propagated through the message activity ID header.</span></span>  
  
 <span data-ttu-id="ff9ae-124">Se `propagateActivity` = `true` e `ActivityTracing` = `off` para um ouvinte de rastreamento de ServiceModel (independentemente de o rastreamento estar habilitado no ServiceModel), acontecerá o seguinte no cliente ou no servidor:</span><span class="sxs-lookup"><span data-stu-id="ff9ae-124">If `propagateActivity`=`true` and `ActivityTracing`=`off` for a ServiceModel trace listener (regardless of whether tracing is enabled on ServiceModel), the following happen on either the client or server:</span></span>  
  
- <span data-ttu-id="ff9ae-125">Na solicitação da operação ou no envio de resposta, a ID da atividade em TLS é propagada para fora do código do usuário até que uma mensagem seja formada.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-125">On operation request or sending response, the activity ID in TLS is propagated out of user code until a message is formed.</span></span> <span data-ttu-id="ff9ae-126">Um cabeçalho de ID de atividade também é inserido na mensagem antes de ser enviado.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-126">An activity ID header is also inserted into the message before it is sent.</span></span>  
  
- <span data-ttu-id="ff9ae-127">Ao receber a solicitação ou receber resposta, a ID da atividade é recuperada do cabeçalho da mensagem assim que o objeto da mensagem recebida é criado.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-127">On receiving request or receiving response, the activity ID is retrieved from the message header as soon as the received message object is created.</span></span> <span data-ttu-id="ff9ae-128">A ID da atividade em TLS é propagada assim que a mensagem desaparece do escopo até que o código do usuário seja atingido.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-128">The activity ID in TLS is propagated as soon as the message disappears from scope until user code is reached.</span></span>  
  
 <span data-ttu-id="ff9ae-129">Essas ações garantem que os rastreamentos de usuário serão exibidos na mesma atividade se a propagação estiver ativada.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-129">These actions guarantee that user traces will appear in the same activity if propagation is on.</span></span> <span data-ttu-id="ff9ae-130">No entanto, ele não faz nenhuma garantia em rastreamentos de ServiceModel.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-130">However, it makes no guarantee on ServiceModel traces.</span></span> <span data-ttu-id="ff9ae-131">Os rastreamentos de ServiceModel ocorrerão em uma atividade de código do usuário somente se o processamento desses rastreamentos for executado no mesmo thread em que a atividade de código do usuário foi definida.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-131">ServiceModel traces occur in a user code activity only if the processing of those traces is executed on the same thread where the user code activity was set.</span></span>  
  
 <span data-ttu-id="ff9ae-132">Em geral, os rastreamentos de ServiceModel podem ser observados nos seguintes locais:</span><span class="sxs-lookup"><span data-stu-id="ff9ae-132">In general, ServiceModel traces can be observed in the following places:</span></span>  
  
- <span data-ttu-id="ff9ae-133">Se o rastreamento de ServiceModel estiver desabilitado, todos os rastreamentos emitidos aparecerão nas atividades do usuário.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-133">If ServiceModel tracing is disabled, all emitted traces appear in user activities.</span></span> <span data-ttu-id="ff9ae-134">Se a propagação estiver habilitada no servidor e no cliente, esses rastreamentos estarão na mesma atividade.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-134">If propagation is enabled at both the server and client, these traces will be in the same activity.</span></span>  
  
- <span data-ttu-id="ff9ae-135">Se o rastreamento de ServiceModel estiver habilitado, mas ActivityTracing estiver desabilitado, os rastreamentos de usuário aparecerão na mesma atividade se a propagação estiver habilitada em ambas as extremidades.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-135">If ServiceModel tracing is enabled, but ActivityTracing is disabled, user traces appear in the same activity if propagation is enabled on both ends.</span></span> <span data-ttu-id="ff9ae-136">Os rastreamentos de ServiceModel aparecem na atividade 0000 padrão, a menos que ocorram no mesmo thread que o processamento de código de usuário em que a atividade está inicialmente definida.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-136">ServiceModel traces appear in the default 0000 activity, unless they occur in the same thread as the user code processing where the activity is initially set.</span></span>  
  
- <span data-ttu-id="ff9ae-137">Se o rastreamento de ServiceModel e ActivityTracing estiverem habilitados, os rastreamentos de usuário aparecerão em atividades definidas pelo usuário e os rastreamentos de ServiceModel aparecerão em atividades definidas pelo ServiceModel.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-137">If both ServiceModel tracing and ActivityTracing are enabled, user traces appear in user-defined activities, and ServiceModel traces appear in ServiceModel-defined activities.</span></span> <span data-ttu-id="ff9ae-138">A propagação ocorre no nível de ServiceModel.</span><span class="sxs-lookup"><span data-stu-id="ff9ae-138">Propagation happens at ServiceModel level.</span></span>
